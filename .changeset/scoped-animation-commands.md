---
"@vizij/orchestrator-wasm": patch
---

Accept controller-scoped animation command paths such as `anim/controller/<controller_id>/player/0/cmd/seek` while preserving the legacy `anim/player/...` broadcast command path.
