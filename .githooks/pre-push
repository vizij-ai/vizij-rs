#!/usr/bin/env bash
# Git pre-push hook: hand off to the shared task runner.
set -euo pipefail

REPO_ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
cd "$REPO_ROOT"

# Run the shared task runner directly (no node/pnpm dependency)
bash scripts/hook-tasks.sh pre-push
