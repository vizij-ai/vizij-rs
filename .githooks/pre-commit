#!/usr/bin/env bash
# Git pre-commit hook: enforce formatting and linting for the Rust workspace
set -euo pipefail

echo "[pre-commit] cargo fmt --all"
cargo fmt --all

echo "[pre-commit] cargo clippy --all-targets -- -D warnings"
cargo clippy --all-targets -- -D warnings

echo "[pre-commit] OK"
